import { Route, Routes } from 'react-router-dom';

export const RouterConfig = () => {
  return (
    <Routes>
      <Route exact path={'dupa'} component={null} />
    </Routes>
  );
};

export default RouterConfig;
